{
  "name": "react-backend",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node ./server/bin/www",
    "start-dev": "PORT=3001 nodemon ./server/bin/www",
    "build": "SKIP_PREFLIGHT_CHECK=true cd client && npm i && npm run build",
    "build-win": "cd client && npm i && npm run build",
    "test": "jest --testPathIgnorePatterns=client"
  },
  "dependencies": {
    "cookie-parser": "~1.4.4",
    "debug": "~2.6.9",
    "express": "~4.17.1",
    "http-errors": "~1.6.3",
    "jest": "^24.8.0",
    "morgan": "~1.9.1",
    "pg": "^7.11.0",
    "pg-hstore": "^2.3.3",
    "pug": "^2.0.4",
    "sequelize": "^5.10.2",
    "sequelize-cli": "^5.5.0",
    "twilio": "^3.33.1"
  }
}
